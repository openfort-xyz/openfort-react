---
'create-openfort': major
---

Required Node.js `^20.19.0 || >=22.12.0` to match the generated Vite toolchain, and stopped before network or filesystem work when the CLI runs on an unsupported runtime. Generated projects now use React, React DOM, React type declarations, and TanStack Query versions that satisfy the Openfort React SDK peer ranges.
