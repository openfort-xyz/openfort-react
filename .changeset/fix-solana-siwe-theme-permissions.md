---
"@openfort/react": patch
---

Fixed SOL amount validation and custom Kora backend routing, preserved typed SIWE errors, isolated modal theme selection per instance, and deprecated the unused `sessionKey` grant-permissions input.
