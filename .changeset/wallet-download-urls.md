---
'@openfort/react': patch
---

Pointed the wallet download links at each wallet's own download page instead of a third-party redirector, and dropped the affiliate/referral query parameters that were attached to the Rainbow links. The "Get the app" QR code now resolves to the wallet vendor's official page.
