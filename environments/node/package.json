{
  "name": "test-node",
  "private": true,
  "type": "module",
  "description": "Loads the BUILT package in Node, through the exports map. publint checks packaging metadata and attw checks types; neither executes the output, so without this nothing proves the shipped entry points actually evaluate outside a browser.",
  "scripts": {
    "test": "node esm.mjs"
  },
  "dependencies": {
    "@openfort/react": "workspace:*",
    "@tanstack/react-query": "^5.101.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "viem": "^2.52.2",
    "wagmi": "^3.6.16"
  }
}
